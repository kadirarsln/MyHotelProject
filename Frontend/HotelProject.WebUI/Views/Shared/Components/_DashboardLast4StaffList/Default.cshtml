@* Last 4 Staff *@

@model List<HotelProject.WebUI.Dtos.StaffDto.ResultLast4StaffDto>

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-lg-3 col-sm-6">
            <div class="card">
                <div class="card-body">
                    <div class="text-center">
                        <img src="@item.SocialMedia1" class="rounded-circle" width="100" height="100" alt="">
                        <h5 class="mt-3 mb-1">@item.Name</h5>
                        <p class="m-0">@item.Tittle</p>
                    </div>
                </div>
            </div>
        </div>
    }
</div>